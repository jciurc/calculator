import { AboutSlint, VerticalBox, Button, TextEdit  } from "std-widgets.slint";
import { Styles } from "styles.slint";

export component MainWindow inherits Window {
    width: 40rem;
    height: 800px;
    background: transparent;

    property <string> value: "3 + (4 * 2)";
    property <string> display: "";

    Rectangle {
        background: #101422;
        border-width: 5px;
        border-color: Styles.gradient;
        border-radius: 30px;
    }

    VerticalLayout {
        padding: Styles.margin;
        TextInput {
            text: value;
            height: 6rem;
            vertical-alignment: center;
            horizontal-alignment: right;

            font-size: 20px;
            font-family: "Lucida Console";
            opacity: 0.6;
        }
        Text {
            height: 12rem;
            vertical-alignment: center;
            horizontal-alignment: right;
            text: display;
            font-size: 30px;
            font-weight: 800;
            font-family: "Lucida Console";

        }

        GridLayout {
            padding: Styles.margin;
            spacing: Styles.margin;

            Row {
                Button { text: "7"; }
                Button { text: "8"; }
                Button { text: "9"; }
                Button { text: "-"; }
                Button { text: "+"; }
            }
            Row {
                Button { text: "4"; }
                Button { text: "5"; }
                Button { text: "6"; }
                Button { text: "*"; }
                Button { text: "/"; }
            }
            Row {
                Button { text: "1"; }
                Button { text: "2"; }
                Button { text: "3"; }
                Button { text: "%"; }
                Button { text: "^"; }
            }
            Row {
                Button { text: "0"; }
                Button { text: "."; }
                Button { text: "C"; }
                Button { text: "="; colspan: 2; }

            }
        }
    }
}
